// Función que suma tres números y devuelve el resultado
function sumarTres(a, b, c) {
    let resultado = a + b + c;
    return resultado;
}

// Utilizar la función con tres parámetros
let resultadoSumaTres = sumarTres(5, 3, 2);
console.log("La suma de tres números es:", resultadoSumaTres);
